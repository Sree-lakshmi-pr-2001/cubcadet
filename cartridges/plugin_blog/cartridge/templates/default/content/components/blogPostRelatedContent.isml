<isif condition="${pdict.content.relatedContent.length > 0}">
    <div class="blog-related-content container">
        <div class="blog-related-content-title h1">
            ${Resource.msg('blog.postpage.checkoutmoreblogposts', 'blog', null)}
        </div>
        <isset name="assets" value="${pdict.content.relatedContent}" scope="pdict" />
        <isinclude template="content/components/blogPostCarousel" />
    </div>
</isif>

