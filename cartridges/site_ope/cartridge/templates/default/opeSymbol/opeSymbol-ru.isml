<link rel="stylesheet" href="${URLUtils.staticURL(URLUtils.CONTEXT_LIBRARY,'','/css/ope-symbol.css')}" />

<iscomment> BACK TO TOP button </iscomment>
<isslot id="ope-symbol-back-to-top-button" description="OPE Symbol 'back to top' button" context="global" />

<iscomment>* * * BEGIN presentation template opeSymbol-ru.isml (Russian) * * *</iscomment>

<iscomment> WELCOME page (top page) - Russian (ru) </iscomment>
    <iscomment>
    Content assets include:
        SNOW symbols table (only content for ru as of August 2020)
    </iscomment>
<div id="top-page" class="page ru">
    <isslot id="ope-symbol-content-welcome-page-ru" description="OPE Symbol top content page (welcome page) - Russian (ru)" context="global" />
</div>

<iscomment> CONTENT (SYMBOLS) pages: SNOW </iscomment>
<iscomment>
    This language only has SNOW content (no welcome content
    or other machine content), so everything is contained in the
    "ope-symbol-content-welcome-page-ru" content asset for now. As
    more content is added, this content slot can be built-out and utilized.
    * slot id = ope-symbol-content-pages-ru
    * slot description = OPE Symbol content pages (symbol pages) - Russian (ru)
    * slot context = global
</iscomment>

<script src="${URLUtils.staticURL(URLUtils.CONTEXT_LIBRARY, '', '/js/ope-symbol.js')}"></script>