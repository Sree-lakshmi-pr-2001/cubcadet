<isset name="replacePartsStr" value="${product.raw.custom['replaces-parts']}" scope="page"/>
<span class="part-replaces-text font-weight-bold">${Resource.msgf('text.partreplaces', 'product', null, ' ') + (replacePartsStr.split(/[ ,]+/).join(', '))}</span>
